{
  "name": "cascade-expense-capture",
  "version": "1.4.0",
  "private": true,
  "description": "Playwright E2E for OCR (image+PDF) + AI Smart Parse + Sheet setup",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "pw:install": "playwright install --with-deps"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2"
  }
}